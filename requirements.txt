# fastapi>=0.95.0,<0.110.0 # Removed
# uvicorn>=0.22.0,<0.28.0 # Removed
onnx>=1.14.0,<1.16.0
onnxruntime>=1.15.0,<1.17.0
torch>=2.0.0,<2.3.0
transformers>=4.30.0,<4.38.0
# python-multipart>=0.0.6 # Removed (FastAPI dependency)
numpy>=1.24.0,<2.0.0
requests>=2.25.0 # Still needed by gdown potentially, keep for now
tqdm>=4.50.0 # Still needed by gdown potentially, keep for now
psutil>=5.9.0 # Used for memory logging
gdown>=4.7.1 # Keep gdown in case LFS fails and we need it as a fallback in streamlit_app
streamlit>=1.30.0
fastapi>=0.95.0,<0.110.0
uvicorn>=0.22.0,<0.28.0
python-multipart>=0.0.6